# config/__init__.py
# Application configuration — loaded once at startup.
# Uses pydantic-settings to read from environment variables / .env file.
# Other packages should import the settings singleton from here.
