# tools/__init__.py
# LangChain tool definitions.
# Each file in this package exports one or more @tool-decorated functions.
# Tools are the "hands" of the agent — they fetch data, compute values,
# or call external APIs. They must be stateless and independently testable.
