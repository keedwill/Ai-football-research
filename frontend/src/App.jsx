// App.jsx — Root component.
// Renders the top-level page. All routing (if added later) lives here.
import React from "react";
import Home from "./pages/Home";
import "./App.css";

function App() {
  return <Home />;
}

export default App;
